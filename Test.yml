apiVersion: v1
kind: Deployment
metadata: log-app
  name: 
spec:
  volumes:
  - name: log-volume
    emptyDir: {}
  containers:
    - name: dummy-log script generator
      volumeMounts:
        - mountPath: /var/log/example
    - name: l
      volumeMounts: /var/log/example
    - name: 


